version: v2

plugins:
  # Go message types
  - local: protoc-gen-go
    out: gen/go
    opt: paths=source_relative

  # Go gRPC service stubs
  - local: protoc-gen-go-grpc
    out: gen/go
    opt: paths=source_relative

